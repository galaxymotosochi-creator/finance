-- Фикс кассовых смен (30.08.2026)
-- Храним реальный номер смены (присваивается при закрытии: закрытые + 1),
-- чтобы раздел «Смены» показывал настоящий №, а не позицию в списке.
ALTER TABLE shifts ADD COLUMN IF NOT EXISTS shift_number INT;
